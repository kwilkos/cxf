<?xml version="1.0"?>
<project name="hello world demo" default="build" basedir=".">

    <import file="../common_build.xml"/>        
        
    <target name="client" description="run demo client" depends="build">
        <property name="param" value=""/>
        <celtixrun classname="demo.callback.client.Client"
            jvmarg1="-Dceltix.config.file=file:///${basedir}/celtix-client.xml"
            param1="${basedir}/wsdl/basic_callback.wsdl" 
            param2="${op}" 
            param3="${param}"/>
    </target> 
        
    <target name="server" description="run demo server" depends="build">
        <celtixrun classname="demo.callback.server.Server" 
            jvmarg1="-Dceltix.config.file=file:///${basedir}/celtix-server.xml"
            param1="${basedir}/wsdl/basic_callback.wsdl"/>
    </target>
        
    <target name="generate.code">
        <echo level="info" message="Generating code using wsdl2java..."/>
        <wsdl2java file="basic_callback.wsdl"/>
    </target>
    
</project>
