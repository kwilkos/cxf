<?xml version="1.0" encoding="UTF-8"?>
<chapter>
  <title>Tables</title>

  <synopsis>Tables in Docbook use a rich markup.</synopsis>

  <sect1>
    <title>Types of Tables</title>

    <para>There are two types of tables in docbook:</para>

    <itemizedlist>
      <listitem>
        <para>Informal tables</para>
      </listitem>

      <listitem>
        <para>Tables</para>
      </listitem>
    </itemizedlist>

    <para>An informal table is simply a table that does not have a title.
    Tables must have a title element and are listed in a list of tables if it
    is generated.</para>
  </sect1>

  <sect1>
    <title>Specifying a Table</title>

    <para>Both informal tables and tables use the same markup elements to
    define their structure. The differences are as follows:</para>

    <itemizedlist>
      <listitem>
        <para>Informal tables are specified using the <sgmltag
        class="element">informaltable</sgmltag> element and tables are
        specified using the <sgmltag class="element">table</sgmltag>
        element.</para>
      </listitem>

      <listitem>
        <para>Informal tables cannot contain a <sgmltag
        class="element">title</sgmltag> element and tables require a <sgmltag
        class="element">title</sgmltag> element.</para>
      </listitem>
    </itemizedlist>

    <para>Creating a table involves the following steps:</para>

    <procedure>
      <step>
        <para>Specify the proper root element.</para>
      </step>

      <step>
        <para>If you are creating a table, add a <sgmltag
        class="element">title</sgmltag> element.</para>
      </step>

      <step>
        <para>Add a <sgmltag class="element">tgroup</sgmltag> element to wrap
        the contents of the table.</para>
      </step>

      <step>
        <para>If needed, add <sgmltag class="element">colspec</sgmltag>
        elements to specify properties for specific columns.</para>
      </step>

      <step>
        <para>If needed, add <sgmltag class="element">spanspec</sgmltag>
        elements to define reusable column spans.</para>
      </step>

      <step>
        <para>If desired, add a <sgmltag>thead</sgmltag> element to specify a
        header row.</para>

        <substeps>
          <step>
            <para>Add a <sgmltag class="element">row</sgmltag> element.</para>
          </step>

          <step>
            <para>Add an <sgmltag class="element">entry</sgmltag> element for
            each column heading.</para>
          </step>
        </substeps>
      </step>

      <step>
        <para>Add a tbody element to contain the body of the table.</para>

        <substeps>
          <step>
            <para>Add one <sgmltag class="element">row</sgmltag> element for
            each row in the table.</para>
          </step>

          <step>
            <para>In the <sgmltag class="element">row</sgmltag> elements, add
            an <sgmltag class="element">entry</sgmltag> element for each value
            that appear in the row.</para>
          </step>
        </substeps>
      </step>
    </procedure>

    <note>
      <para>CeltiXfire documentation does not use table footers.</para>
    </note>

    <sect2>
      <title>Table Root Elements</title>

      <para>Depending on the type of table you are using you will use either
      the informaltable element or the table element as the root element of
      your table. Both elements use the attributes listed in <xref
      linkend="rootattrs" />.</para>

      <table frame="all" id="rootattrs">
        <title>Attributes for <sgmltag class="element">table</sgmltag> Element
        and <sgmltag class="element">informaltable</sgmltag> Element</title>

        <tgroup cols="3" colsep="1" rowsep="1">
          <colspec colwidth="1.5in" />

          <thead>
            <row>
              <entry align="center">Attribute</entry>

              <entry align="center">Description</entry>

              <entry align="center">Values</entry>
            </row>
          </thead>

          <tbody>
            <row>
              <entry><sgmltag class="attribute">colsep</sgmltag></entry>

              <entry>Specifies if a rule will be drawn between the columns in
              the table.</entry>

              <entry><sgmltag class="attvalue">0</sgmltag>, <sgmltag
              class="attvalue">1</sgmltag>(default)</entry>
            </row>

            <row>
              <entry><sgmltag class="attribute">frame</sgmltag></entry>

              <entry>Specifies how the table will be framed.</entry>

              <entry><sgmltag class="attvalue">all</sgmltag>, <sgmltag
              class="attvalue">bottom</sgmltag>, <sgmltag
              class="attvalue">none</sgmltag>, <sgmltag
              class="attvalue">sides</sgmltag>, <sgmltag
              class="attvalue">top</sgmltag>, <sgmltag
              class="attvalue">topbot</sgmltag></entry>
            </row>

            <row>
              <entry><sgmltag class="attribute">orient</sgmltag></entry>

              <entry>Specifies how the table will be oriented in relationship
              to the rest of the text.</entry>

              <entry><sgmltag class="attvalue">land</sgmltag>, <sgmltag
              class="attvalue">port</sgmltag>(default)</entry>
            </row>

            <row>
              <entry><sgmltag class="attribute">rowsep</sgmltag></entry>

              <entry>Specifies if a rule will be drawn between the rows in the
              table.</entry>

              <entry><sgmltag class="attvalue">0</sgmltag>, <sgmltag
              class="attvalue">1</sgmltag>(deafult)</entry>
            </row>

            <row>
              <entry><sgmltag class="attribute">id</sgmltag></entry>

              <entry>Specifies a unique identifier used to reference the
              table.</entry>

              <entry>a unique string</entry>
            </row>

            <row>
              <entry><sgmltag class="attribute">xreflable</sgmltag></entry>

              <entry>Specifies a lable to be used when cross referencing the
              table.<note>
                  <para>This should only be used for an informal table. A
                  crossreferece to a table will use its title as a
                  label.</para>
                </note></entry>

              <entry>a string</entry>
            </row>
          </tbody>
        </tgroup>
      </table>

      <note>
        <para>Attributes not listed in <xref linkend="rootattrs" /> are not
        used in CeltiXfire documentation.</para>
      </note>
    </sect2>

    <sect2>
      <title>The tgroup Element</title>

      <para>The <sgmltag class="element">tgroup</sgmltag> element groups
      together the logical components of a table. It specifies the number of
      columns in a table and contains the column and span specifications used
      by the table. All of the rows, including the header, are defined inside
      of the <sgmltag class="element">tgroup</sgmltag> element.</para>

      <para>While most tables contain a single <sgmltag
      class="element">tgroup</sgmltag> element, it is possible for a complex
      table to have multiple tgroup elements. In such a case, each <sgmltag
      class="element">tgroup</sgmltag> element would define a unique portion
      of table where the columns and spans had different
      specifications.</para>

      <para><xref linkend="tgroupattrs" /> shows the attributes for the
      <sgmltag class="element">tgroup</sgmltag> element. Attributes in the
      <sgmltag class="element">tgroup</sgmltag> element overide the
      corresponding attributes from the root element.</para>

      <table id="tgroupattrs">
        <title>Attributes for the <sgmltag class="element">tgroup</sgmltag>
        Element</title>

        <tgroup cols="3">
          <colspec colwidth="1.5in" />

          <thead>
            <row>
              <entry align="center">Attribute</entry>

              <entry align="center">Description</entry>

              <entry align="center">Values</entry>
            </row>
          </thead>

          <tbody>
            <row>
              <entry><sgmltag class="attribute">colsep</sgmltag></entry>

              <entry>Specifies if a rule will be drawn between the columns of
              the group.</entry>

              <entry><sgmltag class="attvalue">0</sgmltag>, <sgmltag
              class="attvalue">1</sgmltag>(default)</entry>
            </row>

            <row>
              <entry><sgmltag class="attribute">rowsep</sgmltag></entry>

              <entry>Specifies if a rule will be drawn between the rows of the
              group.</entry>

              <entry><sgmltag class="attvalue">0</sgmltag>, <sgmltag
              class="attvalue">1</sgmltag>(default)</entry>
            </row>

            <row>
              <entry><sgmltag>align</sgmltag></entry>

              <entry>Specifies the horizontal alignment of the entries in the
              group.</entry>

              <entry><sgmltag class="attvalue">center</sgmltag>, <sgmltag
              class="attvalue">justify</sgmltag>, <sgmltag
              class="attvalue">right</sgmltag>, <sgmltag
              class="attvalue">left</sgmltag>(default)</entry>
            </row>

            <row>
              <entry><sgmltag>cols</sgmltag></entry>

              <entry>A required attribute that specifies the number of columns
              in the group.</entry>

              <entry>&gt;=1</entry>
            </row>
          </tbody>
        </tgroup>
      </table>

      <note>
        <para>Attributes not listed in <xref linkend="tgroupattrs" /> are not
        used in CeltiXfire documentation.</para>
      </note>
    </sect2>

    <sect2>
      <title>Specifying Column Properties</title>

      <para>If you desire, you can specify a number of the display properties
      for the column in a table by adding <sgmltag
      class="element">colspec</sgmltag> elements to a <sgmltag
      class="element">tgroup</sgmltag> element. A <sgmltag
      class="element">colspec</sgmltag> element defines properties for all of
      the entries in one column in a group. You can set different properties
      for each column in a group.</para>

      <para><xref linkend="colattrs" /> shows the attributes for the <sgmltag
      class="element">colspec</sgmltag> element. Attributes in the colspec
      element overide corresponding attributes in the <sgmltag
      class="element">tgroup</sgmltag> element and the root element.</para>

      <table id="colattrs">
        <title>Attributes for the <sgmltag class="element">colspec</sgmltag>
        Element</title>

        <tgroup cols="3">
          <colspec colwidth="1.5in" />

          <thead>
            <row>
              <entry align="center">Attribute</entry>

              <entry align="center">Description</entry>

              <entry align="center">Values</entry>
            </row>
          </thead>

          <tbody>
            <row>
              <entry><sgmltag class="attribute">colsep</sgmltag></entry>

              <entry>Specifies if a rule will be drawn to the right of entries
              in the column.</entry>

              <entry><sgmltag class="attvalue">0</sgmltag>, <sgmltag
              class="attvalue">1</sgmltag>(default)</entry>
            </row>

            <row>
              <entry><sgmltag class="attribute">rowsep</sgmltag></entry>

              <entry>Specifies if a rule will be drawn between the rows in the
              column.</entry>

              <entry><sgmltag class="attvalue">0</sgmltag>, <sgmltag
              class="attvalue">1</sgmltag>(default)</entry>
            </row>

            <row>
              <entry><sgmltag>align</sgmltag></entry>

              <entry>Specifies the horizontal alignment of the entries in the
              column.</entry>

              <entry><sgmltag class="attvalue">center</sgmltag>, <sgmltag
              class="attvalue">justify</sgmltag>, <sgmltag
              class="attvalue">right</sgmltag>, <sgmltag
              class="attvalue">left</sgmltag>(default)</entry>
            </row>

            <row>
              <entry><sgmltag>colnum</sgmltag></entry>

              <entry>Specifies the number of the column being
              specified.</entry>

              <entry>1&lt;=<replaceable>X</replaceable>&lt;=number of columns
              </entry>
            </row>
          </tbody>
        </tgroup>
      </table>

      <note>
        <para>Attributes not listed in <xref linkend="colattrs" /> are not
        used in CeltiXfire documentation.</para>
      </note>
    </sect2>

    <sect2>
      <title>Defining Spans</title>

      <para></para>
    </sect2>
  </sect1>
</chapter>