<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.1.2//EN"
"http://www.oasis-open.org/docbook/xml/4.1.2/docbookx.dtd">
<article lang="en-US">
  <title>Command Line Tools</title>

  <sect1>
    <title>The Celtix wsdl2java Utility</title>

    <para><application>wsdl2java</application> gives you greater control over
    the code generation process. The syntax of the command is summarized in
    the following illustration.</para>

    <cmdsynopsis>
      <command>wsdl2java</command>

      <group>
        <arg><option>-?</option></arg>

        <arg><option>-help</option></arg>

        <arg><option>-h</option></arg>
      </group>

      <arg><option>-p</option> <arg rep="repeat"><arg
      choice="opt"><replaceable>wsdl-namespace</replaceable>=</arg><replaceable>Package-Name</replaceable></arg></arg>

      <arg><option>-b</option> <arg
      rep="repeat"><replaceable>binding-name</replaceable></arg></arg>

      <arg><option>-d</option>
      <replaceable>output-directory</replaceable></arg>

      <arg><option>-compile</option></arg>

      <arg><option>-classdir</option>
      <replaceable>compile-class-dir</replaceable></arg>

      <arg><option>-client</option></arg>

      <arg><option>-server</option></arg>

      <arg><option>-impl</option></arg>

      <arg><option>-all</option></arg>

      <arg><option>-ant</option></arg>

      <arg><option>-nexclude</option> <arg
      rep="repeat"><replaceable>schema-namespace</replaceable><arg>=<replaceable>java-packagename</replaceable></arg></arg></arg>

      <arg><option>-exsh</option> (true/false)</arg>

      <arg><option>-validate</option></arg>

      <arg><option>-v</option></arg>

      <group>
        <arg><option>-verbose</option></arg>

        <arg><option>-quiet</option></arg>
      </group>

      <arg choice="req"><replaceable>wsdlfile</replaceable></arg>
    </cmdsynopsis>

    <para>The arguments used to manage the code generation process are
    reviewed in the following table.</para>

    <informaltable frame="all">
      <tgroup cols="2">
        <thead>
          <row>
            <entry>Option</entry>

            <entry>Interpretation</entry>
          </row>
        </thead>

        <tbody>
          <row>
            <entry><option>-?</option>
            <para />
			<option>-help</option>
            <para />
			<option>-h</option></entry>

            <entry>Displays the help for this utility.</entry>
          </row>

          <row>
            <entry><synopsis><option>-p</option> <optional><replaceable>wsdl-namespace</replaceable>=</optional><replaceable>Package-name</replaceable></synopsis></entry>

            <entry>Specifies zero, or more, package names to use for the
            generated code. Optionally specifies the WSDL namespace to package
            name mapping.</entry>
          </row>

          <row>
            <entry><option>-b</option>
            <replaceable>binding-file-name</replaceable></entry>

            <entry>Specifies zero, or more, JAXWS or JAXB binding files.
            Use spaces to separate multiple entries.</entry>
          </row>

          <row>
            <entry><option>-d</option>
            <replaceable>output-directory</replaceable></entry>

            <entry>Specifies the directory into which the generated code
            files are written.</entry>
          </row>

          <row>
            <entry><option>-compile</option></entry>

            <entry>Compiles generated Java files.</entry>
          </row>

          <row>
            <entry><option>-classdir</option>
            <replaceable>complile-class-dir</replaceable></entry>

            <entry>Specifies the directory into which the compiled class
            files are written.</entry>
          </row>

          <row>
            <entry><option>-client</option></entry>

            <entry>Generates starting point code for a client
            mainline.</entry>
          </row>

          <row>
            <entry><option>-server</option></entry>

            <entry>Generates starting point code for a server
            mainline.</entry>
          </row>

          <row>
            <entry><option>-impl</option></entry>

            <entry>Generates starting point code for an implementation
            object.</entry>
          </row>

          <row>
            <entry><option>-all</option></entry>

            <entry>Generates all starting point code: types, service
            proxy, service interface, server mainline, client mainline,
            implementation object, and an Ant <filename>build.xml</filename>
            file.</entry>
          </row>

          <row>
            <entry><option>-ant</option></entry>

            <entry>Generates the Ant <filename>build.xml</filename>
            file.</entry>
          </row>

          <row>
            <entry><synopsis><option>-nexclude</option> <replaceable>schema-namespace</replaceable><optional>=<replaceable>java-packagename</replaceable></optional></synopsis></entry>

            <entry>Ignore the specified WSDL schema namespace when
            generating code. This option may be specified multiple times.
            Also, optionally specifies the Java package name used by types
            described in the excluded namespace(s).</entry>
          </row>

          <row>
            <entry><option>-exsh</option>
            (<literal>true</literal>/<literal>false</literal>)</entry>

            <entry>Enables or disables processing of extended soap
            header message binding.</entry>
          </row>

          <row>
            <entry><option>-validate</option></entry>

            <entry>Enables or disables validating the wsdl before
            generating the code.</entry>
          </row>

          <row>
            <entry><option>-v</option></entry>

            <entry>Displays the version number for the
            tool.</entry>
          </row>

          <row>
            <entry><option>-verbose</option></entry>

            <entry>Displays comments during the code generation process.</entry>
          </row>

          <row>
            <entry><option>-quiet</option></entry>

            <entry>Suppresses comments during the code generation process.</entry>
          </row>

          <row>
            <entry><replaceable>wsdlfile</replaceable></entry>

            <entry>The path and name of the WSDL file to use in
            generating the code.</entry>
          </row>
        </tbody>
      </tgroup>
    </informaltable>

    <para>You must specify the absolute or relative path to the WSDL file as
    the last argument. All other arguments are optional and may be listed in
    any order.</para>
  </sect1>

  <sect1>
    <title>The Celtix java2wsdl Utility</title>

    <para><application>java2wsdl</application> uses a Web service endpoint's
    implementation (SEI) class and associated types classes to generate a WSDL
    file. The syntax of the command is summarized in the following
    illustration.</para>

    <cmdsynopsis>
      <command>java2wsdl</command>

      <group>
        <arg><option>-?</option></arg>

        <arg><option>-help</option></arg>

        <arg><option>-h</option></arg>
      </group>

      <arg><option>-o</option> <replaceable>output-file</replaceable></arg>

      <arg><option>-cp</option> <replaceable>class-path</replaceable></arg>

      <arg><option>-t</option>
      <replaceable>target-namespace</replaceable></arg>

      <arg><option>-servicename</option>
      <replaceable>service-name</replaceable></arg>

      <arg><option>-v</option></arg>

      <group>
        <arg><option>-verbose</option></arg>

        <arg><option>-quiet</option></arg>
      </group>

      <arg choice="req"><replaceable>classname</replaceable></arg>
    </cmdsynopsis>

    <para>The arguments used to manage the WSDL file generation are reviewed
    in the following table.</para>

    <informaltable frame="all">
      <tgroup cols="2">
        <thead>
          <row>
            <entry>Option</entry>

            <entry>Interpretation</entry>
          </row>
        </thead>

        <tbody>
          <row>
            <entry><para><option>-?</option></para>
            <para><option>-help</option></para>
            <para><option>-h</option></para></entry>

            <entry><para>Displays the online help for this
            utility.</para></entry>
          </row>

          <row>
            <entry><para><option>-o</option>
            <replaceable>output-file</replaceable></para></entry>

            <entry><para>Specifies the name of the generated WSDL
            file.</para></entry>
          </row>

          <row>
            <entry><para><option>-cp</option>
            <replaceable>class-path</replaceable></para></entry>

            <entry><para>Specify the SEI and types class search path of
            directories and zip/jar files.</para></entry>
          </row>

          <row>
            <entry><para><option>-t</option>
            <replaceable>target-namespace</replaceable></para></entry>

            <entry><para>Specifies the target namespace to use in the
            generated WSDL file.</para></entry>
          </row>

          <row>
            <entry><para><option>-servicename</option>
            <replaceable>service-name</replaceable></para></entry>

            <entry><para>Specifies the value of the generated <sgmltag
            class="element">service</sgmltag> element's <sgmltag
            class="attribute">name</sgmltag> attribute.</para></entry>
          </row>

          <row>
            <entry><option>-v</option></entry>

            <entry><para>Displays the version number for the
            tool.</para></entry>
          </row>

          <row>
            <entry><option>-verbose</option></entry>

            <entry><para>Displays comments during the code generation
            process.</para></entry>
          </row>

          <row>
            <entry><option>-quiet</option></entry>

            <entry><para>Suppresses comments during the code generation
            process.</para></entry>
          </row>

          <row>
            <entry><replaceable>classname</replaceable></entry>

            <entry><para>Specifies the name of the service endpoint
            class.</para></entry>
          </row>
        </tbody>
      </tgroup>
    </informaltable>

    <para>You must include the <replaceable>classname</replaceable> argument.
    All other arguments are optional and may be listed in any order. This tool
    will search and load the service endpoint class and types classes. Make
    certain these classes are on the <envar>CLASSPATH</envar> or in a location
    identified through the <option>-cp</option> flag.</para>
  </sect1>

  <sect1>
    <title>The Celtix wsdl2soap Utility</title>

    <para><application>wsdl2soap</application> will generate a new WSDL file
    with a SOAP binding from an existing WSDL file containing a portType
    element. The syntax of the command is summarized in the following
    illustration.</para>

    <cmdsynopsis>
      <command>wsdl2soap</command>

      <group>
        <arg><option>-?</option></arg>

        <arg><option>-help</option></arg>

        <arg><option>-h</option></arg>
      </group>

      <arg><option>-i</option> <replaceable>port-type-name</replaceable></arg>

      <arg><option>-b</option> <replaceable>binding-name</replaceable></arg>

      <arg><option>-d</option>
      <replaceable>output-directory</replaceable></arg>

      <arg><option>-o</option> <replaceable>output-file</replaceable></arg>

      <arg><option>-n</option>
      <replaceable>soap-body-namespace</replaceable></arg>

      <arg><option>-style</option> (document/rpc)</arg>

      <arg><option>-use</option> (literal/encoded)</arg>

      <arg><option>-v</option></arg>

      <group>
        <arg><option>-verbose</option></arg>

        <arg><option>-quiet</option></arg>
      </group>

      <arg choice="req"><replaceable>wsdlurl</replaceable></arg>
    </cmdsynopsis>

    <para>The arguments used to manage the WSDL file generation are reviewed
    in the following table.</para>

    <informaltable frame="all">
      <tgroup cols="2">
        <thead>
          <row>
            <entry>Option</entry>

            <entry>Interpretation</entry>
          </row>
        </thead>

        <tbody>
          <row>
            <entry><option>-?</option>
            <para />
			<option>-help</option>
            <para />
			<option>-h</option></entry>

            <entry>Displays the online help for this
            utility.</entry>
          </row>

          <row>
            <entry><option>-i</option>
            <replaceable>port-type-name</replaceable></entry>

            <entry>Specifies the <sgmltag
            class="element">portType</sgmltag> element for which a binding
            should be generated.</entry>
          </row>

          <row>
            <entry><para><option>-b</option>
            <replaceable>binding-name</replaceable></para></entry>

            <entry><para>Specifies the name of the generated SOAP
            binding.</para></entry>
          </row>

          <row>
            <entry><para><option>-d</option>
            <replaceable>output-directory</replaceable></para></entry>

            <entry><para>Specifies the directory to place generated WSDL
            file.</para></entry>
          </row>

          <row>
            <entry><para><option>-o</option>
            <replaceable>output-file</replaceable></para></entry>

            <entry><para>Specifies the name of the generated WSDL
            file.</para></entry>
          </row>

          <row>
            <entry><para><option>-n
            </option><replaceable>soap-body-namespace</replaceable></para></entry>

            <entry><para>Specifies the SOAP body namespace when the style is
            RPC.</para></entry>
          </row>

          <row>
            <entry><para><option>-style</option>
            (<literal>document</literal>/<literal>rpc</literal>)</para></entry>

            <entry><para>Specifies the encoding style (document or RPC) to use
            in the SOAP binding. The default is
            <constant>document</constant>.</para></entry>
          </row>

          <row>
            <entry><para><option>-use</option>
            (<literal>literal</literal>/<literal>encoded</literal>)</para></entry>

            <entry><para>Specifies the binding use (encoded or literal) to use
            in the SOAP binding. The default is
            <constant>literal.</constant></para></entry>
          </row>

          <row>
            <entry><option>-v</option></entry>

            <entry><para>Displays the version number for the
            tool.</para></entry>
          </row>

          <row>
            <entry><option>-verbose</option></entry>

            <entry><para>Displays comments during the code generation
            process.</para></entry>
          </row>

          <row>
            <entry><option>-quiet</option></entry>

            <entry><para>Suppresses comments during the code generation
            process.</para></entry>
          </row>

          <row>
            <entry><replaceable>wsdlurl</replaceable></entry>

            <entry><para>The path and name of the WSDL file containing the
            <sgmltag class="element">portType</sgmltag> element
            definition.</para></entry>
          </row>
        </tbody>
      </tgroup>
    </informaltable>

    <para>The<option> -i</option> <replaceable>port-type-name</replaceable>
    and <replaceable>wsdlurl</replaceable> arguments are required. If the
    <option>-style rpc</option> argument is specified, the <option>-n</option>
    <replaceable>soap-body-namspace</replaceable> argument is also required.
    All other arguments are optional and may be listed in any order.</para>
  </sect1>

  <sect1>
    <title>The Celtix wsdl2service Utility</title>

    <para><application>wsdl2service</application> creates a new WSDL file
    containing an HTTP or JMS service definition from an existing WSDL
    document containing a binding element. The syntax of the command is
    summarized below:</para>

    <cmdsynopsis>
      <command>wsdl2service</command>

      <group>
        <arg><option>-?</option></arg>

        <arg><option>-help</option></arg>

        <arg><option>-h</option></arg>
      </group>

      <arg><option>-transport</option> (http/jms)</arg>

      <arg><option>-e</option> <replaceable>service-name</replaceable></arg>

      <arg><option>-p</option> <replaceable>port-name</replaceable></arg>

      <arg choice="req"><option>-n</option>
      <replaceable>binding-name</replaceable></arg>

      <arg><option>-a</option> <replaceable>address</replaceable></arg>

      <group>
        <arg><option>-jds</option> (queue/topic)</arg>

        <arg><option>-jpu</option>
        <replaceable>jndi-provider-URL</replaceable></arg>

        <arg><option>-jcf</option>
        <replaceable>initial-context-factory</replaceable></arg>

        <arg><option>-jfn</option>
        <replaceable>jndi-connection-factory-name</replaceable></arg>

        <arg><option>-jdn</option>
        <replaceable>jndi-destination-name</replaceable></arg>

        <arg><option>-jmt</option> (text/binary)</arg>

        <arg><option>-jmc</option> (true/false)</arg>

        <arg><option>-jsn</option>
        <replaceable>durable-subscriber-name</replaceable></arg>
      </group>

      <arg><option>-o</option> <replaceable>output-file</replaceable></arg>

      <arg><option>-d
      </option><replaceable>output-directory</replaceable></arg>

      <arg><option>-v</option></arg>

      <group>
        <arg><option>-verbose</option></arg>

        <arg><option>-quiet</option></arg>
      </group>

      <arg choice="req"><replaceable>wsdlurl</replaceable></arg>
    </cmdsynopsis>

    <para>The arguments used to manage the WSDL file generation are reviewed
    in the following table.</para>

    <informaltable frame="all">
      <tgroup cols="2">
        <thead>
          <row>
            <entry>Option</entry>

            <entry>Interpretation</entry>
          </row>
        </thead>

        <tbody>
          <row>
            <entry><para><option>-?</option></para>
            <para><option>-help</option></para>
            <para><option>-h</option></para></entry>

            <entry><para>Displays the online help for this
            utility.</para></entry>
          </row>

          <row>
            <entry><para><option>-transport</option>
            (<literal>http</literal>/<literal>jms</literal>)</para></entry>

            <entry><para>Specifies the type of transport to use for the
            generated service.</para></entry>
          </row>

          <row>
            <entry><para><option>-e</option>
            <replaceable>service-name</replaceable></para></entry>

            <entry><para>Specifies the value of the generated <sgmltag
            class="element">service</sgmltag> element's <sgmltag
            class="attribute">name</sgmltag> attribute.</para></entry>
          </row>

          <row>
            <entry><para><option>-p</option>
            <replaceable>port-name</replaceable></para></entry>

            <entry><para>Specifies the value of the generated <sgmltag
            class="element">port</sgmltag> element's <sgmltag
            class="attribute">name</sgmltag> attribute. To specify multiple
            port elements, separate the names by a space.</para></entry>
          </row>

          <row>
            <entry><para><option>-a</option>
            <replaceable>address</replaceable></para></entry>

            <entry><para>Specifies the value used in the <sgmltag
            class="element">address</sgmltag> element of the
            port.</para></entry>
          </row>

          <row>
            <entry><para><option>-n</option>
            <replaceable>binding-name</replaceable></para></entry>

            <entry><para>Specifies the binding used to generate the
            service.</para></entry>
          </row>

          <row>
            <entry><para><option>-jds</option>
            (<literal>queue</literal>/<literal>topic</literal>)</para></entry>

            <entry><para>Specifies the JMS destination style.</para></entry>
          </row>

          <row>
            <entry><para><option>-jpu</option>
            <replaceable>jndi-provider-URL</replaceable></para></entry>

            <entry><para>Specifies the URL of the JMS JNDI
            provider.</para></entry>
          </row>

          <row>
            <entry><para><option>-jcf</option>
            <replaceable>initial-context-factory</replaceable></para></entry>

            <entry><para>Specifies the JMS initial context
            factory.</para></entry>
          </row>

          <row>
            <entry><para><option>-jfn</option>
            <replaceable>jndi-connection-factory-name</replaceable></para></entry>

            <entry><para>Specifies the JMS JNDI connection factory
            name.</para></entry>
          </row>

          <row>
            <entry><para><option>-jdn</option>
            <replaceable>jndi-destination-name</replaceable></para></entry>

            <entry><para>Specifies the JMS JNDI destination
            name.</para></entry>
          </row>

          <row>
            <entry><para><option>-jmt</option>
            (<literal>text</literal>/<literal>binary</literal>)</para></entry>

            <entry><para>Specifies the JMS message type.</para></entry>
          </row>

          <row>
            <entry><para><option>-jmc</option>
            (<literal>true</literal>/<literal>false</literal>)</para></entry>

            <entry><para>Specifies if the <classname>MessageID</classname> is
            used as the <classname>CorrelationID</classname>.</para></entry>
          </row>

          <row>
            <entry><para><option>-jsn</option>
            <replaceable>durable-subscriber-name</replaceable></para></entry>

            <entry><para>Specifies an optional durable subscriber
            name.</para></entry>
          </row>

          <row>
            <entry><para><option>-o</option>
            <replaceable>output-file</replaceable></para></entry>

            <entry><para>Specifies the name of the generated WSDL
            file.</para></entry>
          </row>

          <row>
            <entry><para><option>-d</option>
            <replaceable>output-directory</replaceable></para></entry>

            <entry><para>Specifies the directory in which the generated WSDL
            is placed.</para></entry>
          </row>

          <row>
            <entry><option>-v</option></entry>

            <entry><para>Displays the version number for the
            tool.</para></entry>
          </row>

          <row>
            <entry><option>-verbose</option></entry>

            <entry><para>Displays comments during the code generation
            process.</para></entry>
          </row>

          <row>
            <entry><option>-quiet</option></entry>

            <entry><para>Suppresses comments during the code generation
            process.</para></entry>
          </row>

          <row>
            <entry><replaceable>wsdlurl</replaceable></entry>

            <entry><para>The path and name of the existing WSDL
            file.</para></entry>
          </row>
        </tbody>
      </tgroup>
    </informaltable>

    <para>You must specify the <option>-n</option>
    <replaceable>binding-name</replaceable> argument and the
    <replaceable>wsdlurl</replaceable> argument. All other arguments are
    optional and may be listed in any order.</para>
  </sect1>

  <sect1>
    <title>The Celtix wsdl2xml Utility</title>

    <para><application>wsdl2xml</application> generates an XML binding from an
    existing WSDL document containing a portType definition. The syntax of the
    command is summarized below:</para>

    <cmdsynopsis>
	<command>wsdl2xml</command>
	<group>
        <arg><option>-?</option></arg>

        <arg><option>-help</option></arg>

        <arg><option>-h</option></arg>
      </group>
	<arg><option>-i</option> <replaceable>port-type-name</replaceable></arg>
	<arg><option>-b</option> <replaceable>binding-name</replaceable></arg>
	<arg><option>-e</option> <replaceable>service-name</replaceable></arg>
	<arg><option>-p</option> <replaceable>port-name</replaceable></arg>
	<arg><option>-a</option> <replaceable>address</replaceable></arg>
	<arg><option>-d</option> <replaceable>output-directory</replaceable></arg>
	<arg><option>-o</option> <replaceable>output-file</replaceable></arg>
	<arg><option>-v</option></arg>
	<group>
	<arg><option>-verbose</option></arg><arg><option>-quiet</option></arg>
	</group>
	<arg choice="req"><replaceable>wsdlurl</replaceable></arg>
	</cmdsynopsis>

    <para>The arguments used to manage WSDL file generation are reviewed in
    the following table.</para>

    <informaltable frame="all">
      <tgroup cols="2">
        <thead>
          <row>
            <entry>Option</entry>

            <entry>Interpretation</entry>
          </row>
        </thead>

        <tbody>
          <row>
            <entry><option>-?</option>
            <para />
			<option>-help</option>
            <para /><option>-h</option></entry>

            <entry>Displays the online help for this
            utility.</entry>
          </row>

          <row>
            <entry><option>-i</option>
            <replaceable>port-type-name</replaceable></entry>

            <entry>Specifies the <sgmltag
            class="element">portType</sgmltag> element to use.</entry>
          </row>

          <row>
            <entry><option>-b</option>
            <replaceable>binding-name</replaceable></entry>

            <entry>Specifies the name of the generated XML
            binding.</entry>
          </row>

          <row>
            <entry><option>-e</option>
            <replaceable>service-name</replaceable></entry>

            <entry>Specifies the value of the generated <sgmltag
            class="element">service</sgmltag> element's <sgmltag
            class="attribute">name</sgmltag> attribute.</entry>
          </row>

          <row>
            <entry><option>-p</option>
            <replaceable>port-name</replaceable></entry>

            <entry>Specifies the value of the generated <sgmltag
            class="element">port</sgmltag> element's <sgmltag
            class="attribute">name</sgmltag> attribute. To specify multiple
            <sgmltag class="element">port</sgmltag> elements, separate the
            names by a space.</entry>
          </row>

          <row>
            <entry><option>-a</option>
            <replaceable>address</replaceable></entry>

            <entry>Specifies the value used in the <sgmltag
            class="element">address</sgmltag> element of the generated
            <sgmltag class="element">port</sgmltag> element.</entry>
          </row>

          <row>
            <entry><option>-d</option>
            <replaceable>output-directory</replaceable></entry>

            <entry>Specifies the directory to place generated WSDL
            file.</entry>
          </row>

          <row>
            <entry><option>-o</option>
            <replaceable>output-file</replaceable></entry>

            <entry>Specifies the name of the generated WSDL
            file.</entry>
          </row>

          <row>
            <entry><option>-v</option></entry>

            <entry>Displays the version number for the
            tool.</entry>
          </row>

          <row>
            <entry><option>-verbose</option></entry>

            <entry>Displays comments during the code generation
            process.</entry>
          </row>

          <row>
            <entry><option>-quiet</option></entry>

            <entry>Suppresses comments during the code generation
            process.</entry>
          </row>

          <row>
            <entry><replaceable>wsdlurl</replaceable></entry>

            <entry>The path and name of the existing WSDL
            file.</entry>
          </row>
        </tbody>
      </tgroup>
    </informaltable>

    <para>The <option>-i </option><replaceable>port-type-name</replaceable>
    and the <replaceable>wsdlurl</replaceable> arguments are required. All
    other arguments are optional and may be listed in any order.</para>
  </sect1>

  <sect1>
    <title>The Celtix xsd2wsdl Utility</title>

    <para><application>xsd2wsdl</application> imports an XMLSchema document
    and generates a WSDL file containing a <sgmltag
    class="element">types</sgmltag> element populated by the types defined in
    the XMLSchema document. The syntax of the command is summarized
    below:</para>

    <cmdsynopsis>
	<command>xsd2wsdl</command>
	<group>
        <arg><option>-?</option></arg>

        <arg><option>-help</option></arg>

        <arg><option>-h</option></arg>
      </group>
	<arg><option>-t</option> <replaceable>target-namespace</replaceable></arg>
	<arg><option>-n</option> <replaceable>wsdl-name</replaceable></arg>
	<arg><option>-d</option> <replaceable>output-directory</replaceable></arg>
	<arg><option>-o</option> <replaceable>output-file</replaceable></arg>
	<arg><option>-v</option></arg>
	<group>
	<arg><option>-verbose</option></arg><arg><option>-quiet</option></arg>
	</group>
	<arg choice="req"><replaceable>xsdurl</replaceable></arg>
	</cmdsynopsis>

    <para>The arguments used to manage WSDL file generation are reviewed in
    the following table.</para>

    <informaltable frame="all">
      <tgroup cols="2">
        <thead>
          <row>
            <entry>Option</entry>

            <entry>Interpretation</entry>
          </row>
        </thead>

        <tbody>
          <row>
            <entry><option>-?</option>
            <para />
			<option>-help</option>
            <para />
			<option>-h</option></entry>

            <entry>Displays the online help for this
            utility.</entry>
          </row>

          <row>
            <entry><option>-t</option>
            <replaceable>target-namespace</replaceable></entry>

            <entry>Specifies the target namespace for the generated
            WSDL.</entry>
          </row>

          <row>
            <entry><option>-n</option>
            <replaceable>wsdl-name</replaceable></entry>

            <entry>Specifies the value of the generated <sgmltag
            class="element">definition</sgmltag> element's <sgmltag
            class="attribute">name</sgmltag> attribute.</entry>
          </row>

          <row>
            <entry><option>-d</option>
            <replaceable>output-directory</replaceable></entry>

            <entry>Specifies the directory in which the generated WSDL
            is placed.</entry>
          </row>

          <row>
            <entry><option>-o</option>
            <replaceable>output-file</replaceable></entry>

            <entry>Specifies the name of the generated WSDL
            file.</entry>
          </row>

          <row>
            <entry><option>-v</option></entry>

            <entry>Displays the version number for the
            tool.</entry>
          </row>

          <row>
            <entry><option>-verbose</option></entry>

            <entry>Displays comments during the code generation
            process.</entry>
          </row>

          <row>
            <entry><option>-quiet</option></entry>

            <entry>Suppresses comments during the code generation
            process.</entry>
          </row>

          <row>
            <entry><replaceable>xsdurl</replaceable></entry>

            <entry>The path and name of the existing XSDSchema
            file.</entry>
          </row>
        </tbody>
      </tgroup>
    </informaltable>

    <para>The <option>-t </option><replaceable>target-namespace</replaceable>
    and the <replaceable>xsdurl</replaceable> arguments are required. All
    other arguments are optional and may be listed in any order.</para>
  </sect1>

  <sect1>
    <title>The Celtix wsdlvalidator Utility</title>

    <para><application>wsdlvalidator</application> validates whether a wsdl
    file or wsdl url is well-formed and schema conformed.</para>

    <cmdsynopsis>
	<command>wsdlvalidator</command>
      <group>
        <arg><option>-?</option></arg>

        <arg><option>-help</option></arg>

        <arg><option>-h</option></arg>
      </group>
	  <arg><option>-d</option> <replaceable>schema-directory</replaceable></arg>
	  <arg><option>-s</option> <arg rep="repeat"><replaceable>schema-url</replaceable></arg></arg>
	  <arg><option>-v</option></arg>
	  <group>
	  <arg><option>-verbose</option></arg><arg><option>-quiet</option></arg>
	  </group>
	  <arg choice="req"><replaceable>wsdlurl</replaceable></arg>
	  </cmdsynopsis>

    <para>The arguments used to validate WSDL file are reviewed in the
    following table.</para>

    <informaltable frame="all">
      <tgroup cols="2">
        <thead>
          <row>
            <entry>Option</entry>

            <entry>Interpretation</entry>
          </row>
        </thead>

        <tbody>
          <row>
            <entry><option>-?</option>
            <para />
			<option>-help</option>
            <para />
			<option>-h</option></entry>

            <entry>Displays the online help for this
            utility.</entry>
          </row>

          <row>
            <entry><option>-d</option>
            <replaceable>schema-directory</replaceable></entry>

            <entry>Specifies the directory used to search for
            schemas.</entry>
          </row>

          <row>
            <entry><option>-s</option>
            <replaceable>schema-url</replaceable></entry>

            <entry>Specifies the URL of a user specific schema to be
            included in the validation of the contract. This switch can appear
            multiple times.</entry>
          </row>

          <row>
            <entry><option>-v</option></entry>

            <entry>Displays the version number for the
            tool.</entry>
          </row>

          <row>
            <entry><option>-verbose</option></entry>

            <entry>Displays comments during the
            validation.</entry>
          </row>

          <row>
            <entry><option>-quiet</option></entry>

            <entry>Suppresses comments during the validation.</entry>
          </row>

          <row>
            <entry><replaceable>wsdlurl</replaceable></entry>

            <entry>The path and name of the existing WSDL file</entry>
          </row>
        </tbody>
      </tgroup>
    </informaltable>
  </sect1>
</article>